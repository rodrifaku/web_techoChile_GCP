# Archivo de ejemplo de variables de entorno
# 
# OPCIÓN 1: Para desarrollo local con base de datos local
# Copia este contenido a un archivo llamado .env en la raíz del proyecto
#
# OPCIÓN 2: Para desarrollo local con base de datos remota (IP externa)
# Copia este contenido a un archivo llamado .env.local (NO se sube a git)
# y configura la IP externa en DB_HOST
#
# NUNCA subas los archivos .env o .env.local al control de versiones

# Django Settings
SECRET_KEY=tu-secret-key-super-segura-aqui
DEBUG=True

# Database Configuration
# Para desarrollo local con DB local, usa:
DB_NAME=django_db
DB_USER=django_user
DB_PASSWORD=tu-password-de-base-de-datos-aqui
DB_HOST=127.0.0.1
DB_PORT=5432

# Para desarrollo local con DB remota, en .env.local usa:
# DB_HOST=34.176.25.171  (o la IP externa que necesites)

# Allowed Hosts (opcional, separados por comas)
# ALLOWED_HOSTS=localhost,127.0.0.1,tu-dominio.com

# Email Configuration (opcional)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu-email@gmail.com
# EMAIL_HOST_PASSWORD=tu-password-de-email
